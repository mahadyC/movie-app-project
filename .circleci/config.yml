version: 2
jobs:
  build:
    docker:
      - image: /node:8.15.0-alpine
    steps:
      - checkout
      - run: 
          name: Install dependencies
          command: npm install
      - run:
          name: Run build
          command: npm run build
workflows:
  version: 2
  install_and_build:
    jobs:
      - build
      
